# Sample Data

This directory is for storing sample PDF files and test data.

## Usage

- Place sample PDF forms here for testing
- These files can be committed to Git (unless they contain sensitive data)
- If PDFs are too large, consider using Git LFS

## Note

If you want to exclude PDFs from version control, uncomment this line in `.gitignore`:
```
# data/*.pdf
```
